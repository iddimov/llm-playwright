# Dependencies
node_modules/
package-lock.json # Keep if you want to lock versions, but sometimes ignored in libs, usually kept in apps. I'll keep it commented out to NOT ignore it by default as is standard.
# Actually, I will NOT ignore package-lock.json as it is best practice to commit it.

# Testing
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

# Environment variables
.env
.env.local
.env.*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# IDES
.vscode/
.idea/

# Project specific logs
generator_checkout.log
generator_checkout_v2.log
